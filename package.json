{
  "name": "armstrong-editor",
  "version": "1.0.1",
  "description": "A custom text editor for writing javascript, html, and css code",
  "main": "dist/App.js",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Tarmstrong95/armstrong-editor.git"
  },
  "scripts": {
    "start": "electron-forge start dist/App.js",
    "b:s": "npm run build && npm run start",
    "test": "echo \"Error: no test specified\" && exit 1",
    "package": "npm run build && electron-forge package",
    "make": "npm run build && electron-forge make",
    "publish": "npm run build && electron-forge publish",
    "build": "./node_modules/.bin/tsc"
  },
  "author": "Triston Armstrong",
  "license": "MIT",
  "devDependencies": {
    "@electron-forge/cli": "^6.0.4",
    "@electron-forge/maker-deb": "^6.0.4",
    "@electron-forge/maker-rpm": "^6.0.4",
    "@electron-forge/maker-squirrel": "^6.0.4",
    "@electron-forge/maker-zip": "^6.0.4",
    "@electron-forge/publisher-github": "^6.0.4",
    "@types/electron": "^1.6.10",
    "electron": "^22.0.0",
    "typescript": "^4.9.4"
  },
  "dependencies": {
    "electron-squirrel-startup": "^1.0.0",
    "update-electron-app": "^2.0.1"
  }
}
